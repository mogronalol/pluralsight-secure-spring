spring:
  ssl:
    bundle:
      jks:
        test-client:
          truststore:
            location: "classpath:demo-client-truststore.p12"
            password: "notsecure"
          keystore:
            location: "classpath:demo-client-keystore.p12"
            password: notsecure
            type: PKCS12
        server:
          truststore:
            location: "classpath:demo-server-truststore.p12"
            password: "notsecure"
            type: "PKCS12"